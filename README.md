# reusables

Reusable Python project baseline with a minimal development toolchain.

## What is here

- `uv` project setup
- Linting with `ruff`
- Type checking with `mypy`
- Tests with `pytest`
- Utility `scripts/`

Adapt before using.
